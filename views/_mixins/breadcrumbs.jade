mixin Breadcrumbs(list)
  - var last = list.pop();
  if (list.length)
    div.col-md-9
      ul.breadcrumb
        for item in list
          li
            a(href=item.href) !{item.name}
        li.active !{last.name}
